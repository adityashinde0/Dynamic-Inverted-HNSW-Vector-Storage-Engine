{
  "name": "ps-005-vector-storage-engine",
  "version": "1.0.0",
  "private": true,
  "description": "PS-005: Dynamic Inverted-HNSW Vector Storage Engine with High-Throughput Write Ingestion",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "typecheck": "npm --prefix frontend run typecheck",
    "preview": "npm --prefix frontend run preview"
  }
}
